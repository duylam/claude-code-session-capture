# claude-code-session-capture

A Claude Code plugin marketplace hosting `session-capture`: archives each Claude Code session — transcript and sidecar — into `.swiki/captures/` when the session ends or is cleared, with one path-scoped git commit per capture.
